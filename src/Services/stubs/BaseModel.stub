<?php

namespace App\Models\Base;

use Illuminate\Database\Eloquent\Factories\HasFactory;
{$useClasses}

/**
 {$properties}
 */
class Base{$modelName} extends Base
{
    use HasFactory;
    {$useTraits}

    protected $table = '{$name}';
    protected string $comment = '{$comment}';
    protected $fillable = [{$fillable}];
    {$hidden}
    {$guard_name}

    {$casts}
    {$relations}
}
